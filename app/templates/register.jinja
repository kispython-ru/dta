{% extends 'layout.jinja' %}
{% block content %}
<div class="container">
  <div class="card w-50 m-auto mt-5">
    <form class="card-body" action="/register" method="POST">
      <h5 class="card-title">Register</h5>
      <div class="form-group mb-2">
        <label>Email</label>
        <input name="email" type="text" class="form-control" placeholder="Email" value="{{ form.email.data if form.email.data }}">
        <small class="form-text text-muted">Please, enter email.</small>
      </div>
      {% if form.email.errors %}
        <ul class="errors">
          {% for error in form.email.errors %}
            <li>{{ error }}</li>
          {% endfor %}
        </ul>
      {% endif %}
      <div class="form-group mb-2">
        <label>Password</label>
        <input name="password" type="password" class="form-control" placeholder="Password">
        <small class="form-text text-muted">Please, enter your password.</small>
      </div>
      {% if form.password.errors %}
        <ul class="errors">
          {% for error in form.password.errors %}
            <li>{{ error }}</li>
          {% endfor %}
        </ul>
      {% endif %}
      <div class="form-group mb-2">
        <label>Repeat password</label>
        <input name="password_repeat" type="password" class="form-control" placeholder="Repeat password">
        <small class="form-text text-muted">Please, repeat your password.</small>
      </div>
      {% if form.password_repeat.errors %}
        <ul class="errors">
          {% for error in form.password_repeat.errors %}
            <li>{{ error }}</li>
          {% endfor %}
        </ul>
      {% endif %}
      {{ form.csrf_token }}
      <div class="row">
        <div class="col-md-6">
          <button type="submit" class="btn btn-primary w-100 d-block">
            Register
          </button>
        </div>
        <div class="col-md-6">
          <a href="/login" class="btn btn-secondary w-100 d-block">
            Already registered?
          </a>
        </div>
      </div>
    </form>
  </div>
</div>
{% endblock %}
