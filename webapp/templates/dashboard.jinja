{% extends 'layout.jinja' %}
{% block content %}
<div class="container mt-3">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item active" aria-current="page">Цифровой ассистент преподавателя</li>
    </ol>
  </nav>
  <h5 class="card-title fw-bold">Перечень групп</h5>
  <h6 class="card-subtitle mb-2 text-muted">
    Нажмите на группу, чтобы перейти к вариантам заданий группы.
  </h6>
  <div>
    {% for key, grouping in groupings.items() %}
      <div>
        <small class="mb-2 text-muted d-block">
          <span>{{ key }}</span>
        </small>
        {% for group in grouping %}
          <a class="btn btn-outline-primary mb-2 mr-3" href="/group/{{ group.id }}">
            {{ group.title }}
          </a>
        {% endfor %}
      </div>
    {% endfor %}
  </div>
</div>
{% endblock %}
