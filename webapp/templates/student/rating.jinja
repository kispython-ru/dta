{% extends 'layout.jinja' %}
{% block content %}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse container">
    <ol class="breadcrumb bg-light pt-2 pb-2 m-0">
      <li>
        <a class="logo-container text-decoration-none" href="/">
          <div class="logo">
            <img class="logo-img" src="{{ url_for('static', filename='logo.svg') }}" />
          </div>
        </a>
      </li>
      <li class="breadcrumb-item active" aria-current="page">
        Рейтинг
      </li>
    </ol>
    {% if registration %}
      {% if student is not none %}
        <div class="d-flex gap-3 mt-2 mt-md-0">
            <div>
                <span class="text-muted">{{ student.email | hide }}</span>
                <a class="breadcrumb-item text-decoration-none" href="/logout">
                    Выйти
                </a>
            </div>
            <a class="text-decoration-none" href="/submissions">
                Решения
            </a>
        </div>
      {% else %}
        <a class="breadcrumb-item text-decoration-none" href="/login">
          Войти
        </a>
      {% endif %}
    {% endif %}
  </div>
</nav>
<div class="container mt-3">
  {% if blocked %}
    <h5 class="card-title fw-bold">
      Зачёт ещё не начат
    </h5>
  {% else %}
    <h5 class="card-title fw-bold">
      Рейтинг групп на основании открытых вариантов решения задач
    </h5>
    <h6 class="card-subtitle mb-3 text-muted">
      Подробнее про варианты решения можно прочитать тут.
    </h6>
    <ul class="list-group list-group-numbered" style="overflow-x:auto">
        {% for place in groupings %}
          <li class="list-group-item d-flex justify-content-between align-items-start">
            <a class="text-decoration-none w-100 text-start" href="/group/{{ place.group.id }}">
              <div class="ms-2 me-auto">
              {{ place.group.title }}
            </div>
                </a>
          <span class="badge bg-primary rounded-pill">{{ place.earned }}</span>
          </li>
        {% endfor %}
    </ul>
  {% endif %}
</div>
{% endblock %}
