{% extends 'layout.jinja' %}
{% block content %}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse container">
    <ol class="breadcrumb bg-light pt-2 pb-2 m-0">
      <li>
        <a class="logo-container text-decoration-none" href="/">
          <div class="logo">
            <img class="logo-img" src="{{ url_for('static', filename='logo.svg') }}" />
          </div>
        </a>
      </li>
      <li class="breadcrumb-item">
        <a class="text-decoration-none" href="/group/{{ status.group }}">
          {{ status.group_title }}
        </a>
      </li>
      <li class="breadcrumb-item">
        <a class="text-decoration-none" href="{{ status.submission_url }}">
          №{{ status.task + 1 }}, вариант №{{ status.variant + 1 }}
        </a>
      </li>
      <li class="breadcrumb-item active" aria-current="page">
        Уведомление
      </li>
    </ol>
    {% if student is not none %}
      <span class="breadcrumb-item text-decoration-none">
        <span class="text-muted">{{ student.email }}</span>
        <a class="breadcrumb-item text-decoration-none" href="/logout">
          Выйти
        </a>
      </span>
    {% elif not exam %}
      <a class="breadcrumb-item text-decoration-none" href="/login">
        Войти
      </a>
    {% endif %}
  </div>
</nav>
<div class="container mt-3">
  <h6 class="card-title fw-bold">
    Задача <span class="badge alert-secondary" style="font-size: inherit">№{{ status.task + 1 }}</span>,
    вариант <span class="badge alert-secondary" style="font-size: inherit">№{{ status.variant + 1 }}</span>,
    группа <span class="badge alert-secondary" style="font-size: inherit">{{ status.group_title }}</span>
  </h6>
  <h6 class="card-subtitle mb-2 mt-2 text-muted">
    Ответ на задание успешно отправлен. Результат проверки вскоре отобразится на странице задания.
  </h6>
  <a class="btn btn-outline btn-primary" href="{{ status.submission_url }}">
    Вернуться на страницу задания
  </a>
</div>
{% endblock %}
