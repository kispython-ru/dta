{% extends 'layout.jinja' %}
{% block header %}
<li class="breadcrumb-item active" aria-current="page">
  Главная страница
</li>
<style>
.task-card {
    min-height: 50vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
}
.d-flex {
    min-height: 10vh;
}
</style>
{% endblock %}

{% block main %}
<h4 class="card-title fw-bold">
  {{ greeting_message }}!
</h4>
<h6 class="card-subtitle mb-2 text-muted">
  <div class="mt-4">
    <div class="row">
        {% if group.tasks %}
        <div class="col-md-4">
            <a href="/group/{{ group.id }}/variant/0/task/{{ group.tasks[-1].id }}" class="text-decoration-none text-muted">
                <div class="card text-bg-light mb-3" role="button">
                    <div class="card-body  task-card">
                        <h2 class="card-title">Новая задача: {{ group.tasks[-1].id + 1 }}</h2>
                        {% if group.tasks[-1].formulation is not none %}
                        <p class="card-text">{{ group.tasks[-1].formulation }}</p>
                        {% endif %}
                    </div>
                </div>
            </a>
        </div>
        {% endif %}

        <div class="col-md-{% if group.tasks %}8{% else %}12{% endif %}">
            <div class="row mb-3">
                <div class="col-md-6">
                    <a href="/rating/groups" class="text-decoration-none text-muted">
                        <div class="card p-3">
                            <h5 class="position-absolute top-0 start-0 mt-2 ms-3">Рейтинг групп</h5>
                            <div class="d-flex justify-content-left align-items-center mt-3 gap-1">
                                <h3 class="mb-0 me-2">№1</h3>
                                <h4 class="mb-0 me-2">ИКБО-20-23</h4>
                            </div>
                        </div>
                    </a>
                </div>

                <div class="col-md-6">
                    <a href="/rating" class="text-decoration-none text-muted">
                        <div class="card p-3">
                            <h5 class="position-absolute top-0 start-0 mt-2 ms-3">Рейтинг студентов</h5>
                            <div class="d-flex justify-content-left align-items-center mt-3 gap-1">
                                <h3 class="mb-0 me-2">№2</h3>
                                <h4 class="mb-0 me-2">ИКБО-20-23 Вар. 24</h4>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
            <div class="card tasks-card">
                <div class="card-body">
                    <h5 class="card-title">Ваши задачи</h5>
                    <div>
                        <div class="row row-cols-4 row-cols-md-3 row-cols-sm-2 row-cols-1 g-2">
                            {% for task in group.tasks %}
                                <div class="col">
                                    <a href="/group/{{ group.id }}/variant/0/task/{{ task.id }}"
                                       class="btn
                                        {% if tasks_statuses[task.id] == 3 %}
                                        btn-outline-danger
                                        {% elif tasks_statuses[task.id] in [2, 5, 6] %}
                                        btn-outline-success
                                        {% else %}
                                        btn-outline-secondary
                                        {% endif %} w-100">№{{ task.id + 1 }}</a>
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                    <a href="/group/{{ group.id }}" target="_blank" class="btn btn-outline-primary w-100 mt-3 w-100 mt-3">
                        Посмотреть таблицу группы
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

</h6>
{% endblock %}

{% block footer %}
<div class="container mt-4 mb-4">
  <div class="row opacity-75">
    <div class="col-12 col-md-5 col-lg-4 col-xl-3">
      <div class="text-muted">
        <a class="text-decoration-none" href="https://github.com/true-grue/kispython" target="_blank">
          Программирование на языке Python.
        </a>
        <a class="text-decoration-none text-muted" href="https://vk.com/kis_it_mirea" target="_blank">
          Кафедра корпоративных информационных систем, РТУ МИРЭА
        </a>
      </div>
    </div>
  </div>
</div>
{% endblock %}
